name: Lint json

on:
  workflow_call:

jobs:
  prettier:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 24

      - run: npx prettier@3.7.4 --check "**/*.{json,jsonc}"
