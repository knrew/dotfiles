[module/filesystem]
type = internal/fs
interval = 30

mount-0 = /

format-mounted = <label-mounted>
format-mounted-padding = 0
format-mounted-prefix = 
format-mounted-prefix-padding = 1
format-mounted-prefix-foreground = ${colors.filesystem-format-mounted-prefix-foreground}
format-mounted-prefix-background = ${colors.filesystem-format-mounted-prefix-background}
label-mounted = %{T2}%percentage_used:2%%%{T-}
label-mounted-padding = 1
label-mounted-foreground = ${colors.filesystem-label-mounted-foreground}
label-mounted-background = ${colors.filesystem-label-mounted-background}
